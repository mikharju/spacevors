name: Build SpaceVors

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build ${{ matrix.name }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux x64
            rid: linux-x64

          - name: Windows x64
            rid: win-x64

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore dependencies
        run: dotnet restore src/Game/Game.csproj

      - name: Publish game
        run: |
          dotnet publish src/Game/Game.csproj \
            --configuration Release \
            --runtime ${{ matrix.rid }} \
            --self-contained true \
            --output ./publish/${{ matrix.rid }}

      # -------------------------
      # Linux AppImage
      # -------------------------

      - name: Install LinuxDeploy
        if: matrix.rid == 'linux-x64'
        uses: miurahr/install-linuxdeploy-action@v1.8.0
        with:
          arch: x64
          plugins: appimage

      - name: Prepare Linux AppImage
        if: matrix.rid == 'linux-x64'
        run: |
          mkdir -p AppDir/usr/bin
          mkdir -p AppDir/usr/share/applications
          mkdir -p AppDir/usr/share/icons/hicolor/256x256/apps

          cp -a ./publish/linux-x64/. AppDir/usr/bin/

          cat > AppDir/usr/share/applications/spacevors.desktop <<'EOF'
          [Desktop Entry]
          Type=Application
          Name=SpaceVors
          Comment=SpaceVors
          Exec=SpaceVors
          Icon=spacevors
          Categories=Game;
          Terminal=false
          EOF

          # linuxdeploy expects the executable itself as the application entry point.
          # Adjust this if the generated executable has a different name.
          chmod +x AppDir/usr/bin/Game

      - name: Create Linux AppImage
        if: matrix.rid == 'linux-x64'
        run: |
          export OUTPUT="SpaceVors-x86_64.AppImage"

          linuxdeploy \
            --appdir AppDir \
            --executable AppDir/usr/bin/Game \
            --desktop-file AppDir/usr/share/applications/spacevors.desktop \
            --output appimage

          mv *.AppImage "$OUTPUT"

      - name: Verify Linux AppImage
        if: matrix.rid == 'linux-x64'
        run: |
          test -f SpaceVors-x86_64.AppImage
          chmod +x SpaceVors-x86_64.AppImage
          file SpaceVors-x86_64.AppImage

      # -------------------------
      # Windows
      # -------------------------

      - name: Package Windows build
        if: matrix.rid == 'win-x64'
        run: |
          cd ./publish/win-x64
          zip -r ../../SpaceVors-windows-x64.zip .

      # -------------------------
      # Upload artifacts
      # -------------------------

      - name: Upload Linux AppImage
        if: matrix.rid == 'linux-x64'
        uses: actions/upload-artifact@v4
        with:
          name: SpaceVors-linux-x64
          path: SpaceVors-x86_64.AppImage
          if-no-files-found: error
          retention-days: 30

      - name: Upload Windows build
        if: matrix.rid == 'win-x64'
        uses: actions/upload-artifact@v4
        with:
          name: SpaceVors-windows-x64
          path: SpaceVors-windows-x64.zip
          if-no-files-found: error
          retention-days: 30
